# Pull Request Summary

P1-1.5 closes the reviewed public data leaks, removes the externally exported development bootstrap helper, validates login redirects, hardens PostgreSQL function execution/search paths, and adds CI validation.
